# Project.graph — 项目全局架构与需求映射
# 生成者: @Meta-Planner
# 生成时间: 2026-05-19T00:00:00Z
# 版本: 1.0.0

## 一、需求摘要

### 需求描述
为管理员"新建用户"对话框（Admin New User Dialog）添加邮件/手机验证码功能，仿照登录页面的验证码登录流程。

### 当前状态 (As-Is)
- **登录页验证码流程**：完整的双标签设计（密码登录 + 验证码登录），支持两步验证（发送验证码 → 输入验证码 → 验证通过登录）
- **管理员用户管理页**：位于 `/admin/users`，由 `UserManagementComponent` 负责渲染，内含新建/编辑/查看用户的对话框
- **新建用户对话框**：当前直接收集 Name、Email、Phone、Password、Role 字段，调用 `POST /v1/admin/users` 创建用户，无验证码环节

### 目标状态 (To-Be)
- 新建用户对话框增加验证码流程：Step 1 → 发送验证码到新用户邮箱/手机 → Step 2 → 输入6位验证码 → Step 3 → 设置密码并提交
- 后端新增 `POST /v1/admin/users/send-code` 端点用于发送验证码
- 后端 `POST /v1/admin/users` 端点支持接收 `verificationCode` 字段，验证通过后才创建用户

## 二、架构概览

```
┌──────────────────────────────────────────────────────────────────────┐
│                      Admin New User Dialog (Frontend)                 │
│  UserManagementComponent                                              │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │  Step 1: Send Code                                            │   │
│  │  ┌──────────────┐ ┌─────────────────┐ ┌──────────────┐       │   │
│  │  │ Contact Type │ │ Contact Input   │ │ Send Button  │       │   │
│  │  │ (Email/Phone)│ │ (email/phone)   │ │ (60s cooldown)│       │   │
│  │  └──────────────┘ └─────────────────┘ └──────────────┘       │   │
│  ├───────────────────────────────────────────────────────────────┤   │
│  │  Step 2: Verify Code                                          │   │
│  │  ┌─────────────────┐ ┌──────────────┐ ┌──────────────┐       │   │
│  │  │ 6-Digit Input   │ │ Verify Button│ │ Resend (Xs)  │       │   │
│  │  └─────────────────┘ └──────────────┘ └──────────────┘       │   │
│  ├───────────────────────────────────────────────────────────────┤   │
│  │  Step 3: Set Password & Submit                                │   │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │   │
│  │  │ Password     │ │ Confirm Pwd  │ │ Submit Button│          │   │
│  │  └──────────────┘ └──────────────┘ └──────────────┘          │   │
│  └───────────────────────────────────────────────────────────────┘   │
│           │ API Calls:                                                │
│           │ 1. POST /v1/admin/users/send-code                        │
│           │ 2. POST /v1/admin/users (with verificationCode)          │
└───────────┼──────────────────────────────────────────────────────────┘
            │
┌───────────▼──────────────────────────────────────────────────────────┐
│                      Backend (NestJS)                                  │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  AdminUsersController                                         │    │
│  │  ┌──────────────────────┐  ┌──────────────────────────────┐  │    │
│  │  │ POST admin/users/    │  │ POST admin/users             │  │    │
│  │  │ send-code            │  │ (modified: +verificationCode)│  │    │
│  │  └────────┬─────────────┘  └──────────────┬───────────────┘  │    │
│  │           │                               │                   │    │
│  │  ┌────────▼───────────────────────────────▼───────────────┐  │    │
│  │  │  AdminUsersService                                       │  │    │
│  │  │  ├── sendVerificationCode() → VerificationService        │  │    │
│  │  │  └── createUser() → verify code → Prisma create          │  │    │
│  │  └──────────────────────────────────────────────────────────┘  │    │
│  └──────────────────────────────────────────────────────────────┘    │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  VerificationService (already exists — reused)                │    │
│  │  ├── generateCode(key, type) → Redis: verify:admin:create:*   │    │
│  │  ├── verifyCode(key, code, type) → Lua atomic verify          │    │
│  │  └── max 3 attempts, 300s TTL                                 │    │
│  └──────────────────────────────────────────────────────────────┘    │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  Infrastructure                                                │    │
│  │  ├── Redis: verification code storage (5 min TTL)             │    │
│  │  ├── EmailService: send verification email (SMTP)             │    │
│  │  └── Rate Limiter: @RateLimit decorator on send-code          │    │
│  └──────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────┘
```

## 三、组件职责

| 组件 | 职责 | 变更类型 |
|------|------|----------|
| **UserManagementComponent** | 添加验证码两步流程（发送 + 验证），步骤指示器，倒计时 | **MODIFY** |
| **AdminStore** (`admin.store.ts`) | 添加 `sendCreateUserCode()`, `verifyAndCreateUser()` 异步方法 | **MODIFY** |
| **AdminService** (`admin.service.ts`) | 添加 `sendCode()`, `createUserWithCode()` HTTP 方法 | **MODIFY** |
| **AdminDTO** (`admin.dto.ts`) | 添加 `SendCreateUserCodeRequest`, `VerifyAndCreateUserRequest` DTO | **MODIFY** |
| **AdminUsersController** | 新增 `sendCode` 端点，修改 `create` 端点 | **MODIFY** |
| **AdminUsersService** | 新增 `sendVerificationCode()` 方法，修改 `createUser()` 方法 | **MODIFY** |
| **VerificationService** | 复用现有验证码生成/验证逻辑（类型参数: `ADMIN_CREATE_USER`） | **REUSE** |
| **EmailService** | 复用现有邮件发送能力 | **REUSE** |
| **Rate Limiter** | 为 send-code 端点添加 `@RateLimit` 装饰器 | **MODIFY** |
| **contract.yaml** | 记录新端点和修改后的 create 端点 | **MODIFY** |

## 四、数据模型变更

### Redis 新 Key 模式
```
verify:admin:create:{contactType}:{contactHash}
Value: { "code": "123456", "attempts": 0, "used": false, "createdAt": "..." }
TTL: 300秒
```

### Prisma Schema
- **无变更** — User 模型已有 email、phone、emailHash、phoneHash 字段，无需新增

### API DTO 变更

**新增: SendCreateUserCodeDto**
```typescript
{
  contactType: ContactType;  // EMAIL | PHONE
  email?: string;            // 当 contactType=EMAIL 时必填
  phone?: string;            // 当 contactType=PHONE 时必填
}
```

**修改: CreateAdminUserDto (增加验证码字段)**
```typescript
{
  name: string;
  email: string;
  phone?: string;
  role: AdminUserRole;       // CUSTOMER | ADMIN | SUPER_ADMIN
  password?: string;         // 可选（验证码登录用户可不设密码）
  verificationCode: string;  // NEW: 6位验证码，发送验证码后获得
}
```

## 五、API 变更

### 新增端点

| 方法 | 路径 | 鉴权 | 描述 |
|------|------|------|------|
| POST | `/v1/admin/users/send-code` | Bearer SUPER_ADMIN | 发送创建用户验证码 |

### 修改端点

| 方法 | 路径 | 变更 |
|------|------|------|
| POST | `/v1/admin/users` | 请求体增加 `verificationCode` 字段；验证码验证通过后才创建用户 |

## 六、状态管理变更

### AdminStore 新增方法
```typescript
// 异步方法
sendCreateUserCode(dto: SendCreateUserCodeRequest): Observable<SendCodeResponse>
verifyAndCreateUser(dto: CreateAdminUserRequest): Observable<AdminUserDto>
```

### UserManagementComponent 新增本地信号
```typescript
codeStep: WritableSignal<1 | 2 | 3>;   // 步骤指示器
contactType: WritableSignal<ContactType>;
countdown: WritableSignal<number>;       // 60s 倒计时
showAntiEnumMessage: WritableSignal<boolean>;
```

## 七、安全设计

| 关注点 | 实现方案 |
|--------|----------|
| **防枚举攻击** | send-code 始终返回 200；用户不存在时仅不返回 maskedContact |
| **防时序攻击** | 用户不存在时添加 100ms 人为延迟 |
| **防重放攻击** | 验证码使用后 Redis Lua 原子删除 |
| **暴力破解防护** | 最多 3 次验证尝试，超限后验证码作废 |
| **速率限制** | send-code: auth 级别 (5次/分钟)，verify: 10次/分钟 |
| **权限控制** | send-code 和 create 均要求 SUPER_ADMIN 角色 |

## 八、技术债扫描结果

| Tech-Debt ID | 描述 | 截止日期 | 距离今天 | 动作 |
|--------------|------|----------|----------|------|
| TECH-001 | Chart调色板同步 | 2026-05-20 | 1天 | **已偿还** ✅，无需动作 |
| TECH-002 | Backend price字段类型 | 2026-06-30 | 42天 | >7天，跳过 |
| TECH-003 | i18n字典参数化 | — | — | Waived，跳过 |
| WV-2026-005 | @Architect 部署 DEPRECATED.md 文件 | 2026-05-25 | 6天 | **≤7天，生成独立偿还任务 T-TECHDEBT-WV-005** |

## 九、测试策略

| 测试类型 | 覆盖范围 | 工具 |
|----------|----------|------|
| **单元测试 (BE)** | AdminUsersService.sendVerificationCode, createUser | Jest + Supertest |
| **单元测试 (FE)** | UserManagementComponent 验证码流程, AdminStore 新方法 | Jest |
| **集成测试** | POST /v1/admin/users/send-code + POST /v1/admin/users 完整流程 | Jest + Supertest |
| **E2E 测试** | 管理员新建用户完整验证码流程 | Playwright |

## 十、风险与缓解

| 风险 | 影响 | 缓解策略 |
|------|------|----------|
| 验证码邮件延迟/丢失 | 用户无法创建 | 60s 重发机制，5分钟 TTL |
| Redis 不可用 | 验证码无法生成 | VerificationService 已有 503 异常处理 |
| 前端步骤状态丢失 | 对话框关闭后重新打开需重新验证 | resetForm() 清空所有状态 |
| ADMIN 角色可见 New User 按钮但后端限制 SUPER_ADMIN | UI误导 | 现有问题，本次不修复（前端 roleGuard 需单独修复） |
