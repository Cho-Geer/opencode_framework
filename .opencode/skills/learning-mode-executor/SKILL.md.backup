---
name: "learning-mode-executor"
description: "Transforms single Q&A into structured learning cases with planning, logging, and knowledge base integration. Invoke when user enters /learn command."
---

# 学习模式执行器 (Learning Mode Executor)

**版本**: 1.0.0
**维护者**: Harness Engineering Team
**触发指令**: `/learn`

## 1. 概述与目标

该 Skill 旨在将 Agent 的**单次问答交互**转变为**结构化的学习案例**，用于系统性研究 Agent Harness Engineering。

当用户使用 `/learn` 指令时，Skill 将接管 Agent 的标准执行流程，对整个任务生命周期进行**规划、记录、复盘与归档**。核心目标是产出高质量的、可检索的经验资产，而非简单的对话记录。

## 2. 触发与边界条件

| 项目 | 规范 |
| :--- | :--- |
| **触发指令** | 严格匹配输入字符串的开头部分：`/learn` (不区分大小写，但要求为独立命令或后跟空格) |
| **误触发保护** | 中间提及 `加入知识库` 或 `/learn` 不作为前缀出现时 **不触发**。 |
| **停止指令** | 任务执行中若用户输入 `/cancel_learn`，则触发异常中断归档流程。 |

## 3. 执行流程控制 (三段式流水线)

### Phase 1: 规划与物化 (Planning)

1. **生成结构化清单**：调用 LLM 生成包含 `1.问题核心拆解`、`2.执行步骤预判`、`3.预期产出物` 的 Markdown 清单。
2. **文件系统物化**：
   - 文件名规则：`YYYYMMDD_HHMM_摘要前10字_SessionID.md`
   - 写入 YAML Frontmatter 头部，包含状态追踪元数据。
3. **状态初始化**：状态标记为 `IN_PROGRESS`。

### Phase 2: 执行与日志注入 (Execution)

1. **钩子注入**：Agent 执行核心循环时，必须挂载以下日志钩子：
   - `on_thought`：记录决策思维链。
   - `on_tool_start`：记录工具名称与意图参数。
   - `on_tool_end`：记录观察结果（**自动裁剪至前 500 字符**，防止日志膨胀）。
   - `on_error`：记录异常堆栈。
2. **实时落盘**：每产生一条日志，立即追加写入 Phase 1 生成的 `.md` 文件。
3. **结果写入**：任务结束后，将最终输出写入文件底部。

### Phase 3: 归纳与闭环 (Finalize)

1. **读取原始日志**：加载 Phase 2 生成的完整过程记录。
2. **生成复盘摘要**：调用 LLM 对原始日志进行**精炼**，提取关键决策、遇到的障碍及解决方案。
3. **生成终版报告**：重写文件为 `_FINAL.md`，包含原始日志与复盘摘要。
4. **记忆库闭环 (关键步骤)**：
   - **动作**：调用环境中已配置的 `Write_to_KB` 工具或其等效实现。
   - **内容**：上传完整的 `_FINAL.md` 内容。
   - **元数据**：必须携带标签 `#Agent/Self_Reflection`、`#Harness_Engineering`、`#Case_Study` 以及执行状态 (`SUCCESS` / `INTERRUPTED`)。

## 4. 异常与中断处理逻辑

无论任务因何种原因中断（网络错误、Token 超限、用户取消），必须执行以下 **Finally 块**：

1. **状态更新**：将 Frontmatter 中的 `status` 改为 `INTERRUPTED`。
2. **原因记录**：在文件末尾追加 `## ⚠️ 流程异常中断` 章节，写入时间戳与错误原因。
3. **生成快照**：确保文件已保存，生成 `_CRASH_ARCHIVE.md` 副本以备查验。

## 5. 生成文件规范

### 文件命名格式
`{YYYYMMDD}_{HHMM}_{Task_Summary_10_Chars}_{SessionID}_FINAL.md`

### 文件内容结构模板

```yaml
---
session_id: abc123
created_at: 2026-04-17T10:30:00Z
task: 用户原始任务描述
phase: FINALIZED
status: SUCCESS
---
# 📋 执行规划清单
[LLM 生成的初始计划]

---

# 📝 执行过程记录 (ReAct Log)
[实时写入的 思考/动作/观察 日志流]

---

# 🔬 复盘与归纳 (AI 生成)
### 1. 执行过程摘要
### 2. 遇到的问题及解决策略
### 3. 可复用的经验 (Harness Engineering 视角)
```

## 6. 依赖项与实现指引

该 Skill 依赖底层 Agent 框架提供以下能力接口，具体实现需根据技术栈进行适配：

| 抽象能力 | LangChain 对应实现 | Mastra 对应实现 | Aster/Dify 对应实现 | Basic Memory (MCP) |
|----------|-------------------|-----------------|---------------------|-------------------|
| 写入知识库 | VectorStore.add_documents() | memory.semanticRecall() | Write to Knowledge Base Tool | write_note() Tool |
| 回调钩子 | BaseCallbackHandler | onStepFinish 钩子 | Workflow 节点监听 | 脚本模拟 |
| 文件 I/O | FileSystemTool | fs 模块 | 内置文件节点 | 本地 Markdown 文件 |

## 7. 测试用例

### 标准流程测试：
**输入**：/learn 帮我分析这段代码的时间复杂度
**预期**：生成 _FINAL.md，且最后一行输出提示"经验已存入长期记忆"。

### 误触发测试：
**输入**：请帮我把这段文本加入知识库
**预期**：不激活 Skill，正常进行对话或工具调用。

### 中断恢复测试：
执行中途强行断开网络，再次查看文件夹。
**预期**：存在包含 INTERRUPTED 标记的 .md 存档文件。
