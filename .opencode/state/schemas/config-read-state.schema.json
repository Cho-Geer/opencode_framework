{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "config-read-state.schema.json",
  "title": "ConfigReadState",
  "description": "Sub-state for config_read_attest MCP tool. Stores per-session attestation records in a nested sessions map (keyed by sessionID) to prevent race conditions under concurrent multi-agent execution. Uses dbAtomicWriteSubState for atomic append within a SQLite transaction.",
  "type": "object",
  "properties": {
    "sessions": {
      "type": "object",
      "description": "Per-session attestation records. Key = sessionID, value = attestation data",
      "additionalProperties": {
        "$ref": "#/$defs/ConfigReadSession"
      }
    }
  },
  "required": ["sessions"],
  "$defs": {
    "ConfigReadSession": {
      "type": "object",
      "properties": {
        "session_id": { "type": "string" },
        "agent": { "type": "string" },
        "attested_at": { "type": "string", "format": "date-time" },
        "files": { "type": "array", "items": { "type": "string" } },
        "verified": { "type": "boolean" },
        "unread_files": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["session_id", "attested_at", "files", "verified"]
    }
  }
}
